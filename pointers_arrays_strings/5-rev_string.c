#include "main.h"

/**
 * rev_string - function that reverses a string
 * @s: string
 * Return: 0
 */

void rev_string(char *s)
{
	int c = 0, i = 0;

	while (s[c] != '\0')
	{
		c++;
	}

	for (i = (c - 1); i <= 0; i--);
}
