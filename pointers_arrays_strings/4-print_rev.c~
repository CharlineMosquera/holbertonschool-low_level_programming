#include "main.h"

/**
 * print_rev - function that prints a string, in reverse
 * @s: string
 * Return: 0
 */

void print_rev(char *s)
{
	int c;

	for (c = 0; str[c] != '\0'; c++)
	{
		r = rev str[c];
		_putchar(r);
	}
	_putchar('\n');
}
