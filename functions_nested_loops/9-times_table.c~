#include "main.h"

/**
 * times_table - prints the 9 times table, starting with 0
 * Return: 0
 */

void times_table(void)
{
	int a, result;
	result *= a;

	for (a = 0; a <= 9; a++)
	{
		_putchar("result");
		_putchar(',');
		_putchar(' ');
}
